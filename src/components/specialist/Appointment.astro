<div class="appointment">
  <div class="info">
    <p>Siguiente cita:</p>
    <p>02:30 AM</p>
    <p>Marzo 10, 2023 | Miércoles</p>
  </div>
  <button>{"Ver >"}</button>
</div>

<style>
  .appointment {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--small-padding);
    background-color: var(--light);
    border-radius: var(--small-radius);
    border-radius: 1rem;
  }

  .info {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .info p:nth-child(1) {
    font-size: 1.25rem;
  }

  .info p:nth-child(2) {
    font-size: 2rem;
    font-weight: bold;
  }

  .info p:nth-child(3) {
    font-size: 1rem;
  }

  .appointment button {
    display: flex;
    background-color: var(--primary-900);
    color: var(--light);
    border: none;
    border-radius: 0.5rem;
    padding: 0.5rem 1.5rem;
    font-weight: bold;
    cursor: pointer;
  }
</style>
